@{
    ViewData["Title"] = "Home Page";
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EcoSphere | Ana Sayfa</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #e0f7fa, #f1f8e9);
            color: #333;
        }

        .hero {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
            padding: 20px;
        }

            .hero h1 {
                font-size: 3rem;
                color: #2e7d32;
                margin-bottom: 20px;
                text-shadow: 1px 1px 3px rgba(0,0,0,0.1);
            }

            .hero p {
                font-size: 1.2rem;
                max-width: 600px;
                line-height: 1.6;
                color: #555;
            }

            .hero .btn {
                margin-top: 30px;
                display: inline-block;
                padding: 12px 24px;
                background: #2e7d32;
                color: #fff;
                text-decoration: none;
                border-radius: 25px;
                font-weight: 600;
                box-shadow: 0 4px 8px rgba(0,0,0,0.1);
                transition: background 0.3s, transform 0.3s;
            }

                .hero .btn:hover {
                    background: #1b5e20;
                    transform: translateY(-2px);
                }

        .wave {
            position: absolute;
            bottom: 0;
            width: 100%;
            height: 150px;
            background: url('https://www.svgrepo.com/show/475724/wave.svg') repeat-x;
            background-size: contain;
            animation: wave 10s linear infinite;
        }

       
    </style>
</head>
<body>
    <div class="hero">
        <h1>EcoSphere Projesi</h1>
        <p>Doğanın korunması ve sürdürülebilirlik için teknolojiyi bir araya getiriyoruz. EcoSphere, çevresel farkındalığı artırmayı ve yeşil gelecek için çözümler sunmayı hedefler.</p>
        <a href="#" class="btn">Projeyi Keşfet</a>
    </div>
    <div class="wave"></div>
</body>
</html>